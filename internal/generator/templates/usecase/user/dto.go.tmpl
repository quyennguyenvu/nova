package user

import "time"

// Input DTOs - what the use case needs (no json/validate tags!)
// The transport layer owns serialization. The assembler maps between them.

type CreateUserInput struct {
	Email    string
	Name     string
	Password string
}

type UpdateUserInput struct {
	Name string
}

// UserOutput is the use-case's return shape — handed back to the transport layer for projection.
type UserOutput struct {
	ID        int64
	Email     string
	Name      string
	CreatedAt time.Time
	UpdatedAt time.Time
}
