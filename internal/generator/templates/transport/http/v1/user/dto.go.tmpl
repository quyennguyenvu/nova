package user

import "time"

type CreateUserRequest struct {
	Email    string `json:"email"    validate:"required,email"`
	Name     string `json:"name"     validate:"required,min=2,max=100"`
	Password string `json:"password" validate:"required,min=8"`
}

type UpdateUserRequest struct {
	Name string `json:"name" validate:"omitempty,min=2,max=100"`
}

// ListQuery is the pagination input for GET /api/v1/users, bound from the query
// string. It carries both `query:` (fiber, echo) and `form:` (gin) tags so this
// one shared DTO works with every framework's native query binder. There is no
// min/max here on purpose — limit/offset bounds are the usecase's policy (so
// gRPC/CLI inherit the same caps); this layer only parses.
type ListQuery struct {
	Email  string `query:"email"  form:"email"  validate:"omitempty,email"`
	Limit  int    `query:"limit"  form:"limit"`
	Offset int    `query:"offset" form:"offset"`
}

// LoginRequest is the credential payload for POST /api/v1/login.
type LoginRequest struct {
	Email    string `json:"email"    validate:"required,email"`
	Password string `json:"password" validate:"required"`
}

// LoginResponse follows the RFC 6749 token-endpoint shape (access_token,
// token_type, expires_in) so OAuth-aware clients can consume it directly.
type LoginResponse struct {
	AccessToken string        `json:"accessToken"`
	TokenType   string        `json:"tokenType"`
	ExpiresIn   int64         `json:"expiresIn"`
	User        *UserResponse `json:"user"`
}

// UserResponse is the HTTP response body for user reads — this layer owns the json tags.
type UserResponse struct {
	ID        int64     `json:"id"`
	Email     string    `json:"email"`
	Name      string    `json:"name"`
	CreatedAt time.Time `json:"createdAt"`
	UpdatedAt time.Time `json:"updatedAt"`
}
