package {{.Package}}

// {{.Title}}Message is the wire shape of the {{.Lower}} event. Keep it in
// lockstep with the publisher's message DTO (same fields, same json tags).
type {{.Title}}Message struct {
	// TODO: add fields
}
