package {{.Package}}

import (
	"net/http"

	"github.com/gin-gonic/gin"
)

type {{.Title}}Handler struct {
	// TODO: inject {{.Lower}} use case service
}

func New{{.Title}}Handler() *{{.Title}}Handler {
	return &{{.Title}}Handler{}
}

func (h *{{.Title}}Handler) Create(c *gin.Context) {
	// TODO: implement
	c.JSON(http.StatusCreated, gin.H{"status": "created"})
}

func (h *{{.Title}}Handler) GetByID(c *gin.Context) {
	// TODO: implement
	c.JSON(http.StatusOK, gin.H{"status": "ok"})
}

func (h *{{.Title}}Handler) Update(c *gin.Context) {
	// TODO: implement
	c.JSON(http.StatusOK, gin.H{"status": "updated"})
}

func (h *{{.Title}}Handler) Delete(c *gin.Context) {
	// TODO: implement
	c.Status(http.StatusNoContent)
}
