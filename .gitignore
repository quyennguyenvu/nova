# IDE
.idea
.vscode
.DS_Store

# Config
.env

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
nova
!nova/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/
out/

# knowledge graph
# Tracks local API token spend (never commit)
graphify-out/cost.json
# Internal file system paths (unreliable across different machines)
graphify-out/manifest.json

# Optional: keep repo size small, or commit it to speed up team parsing
graphify-out/cache/
